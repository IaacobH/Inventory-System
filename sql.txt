INSERT INTO products (name, price, stock, category_id)
VALUES ('apple',80,12,2);

SELECT * FROM products;

SELECT * FROM products ORDER BY price DESC;


SELECT name, price FROM products;

SELECT * FROM products WHERE stock < 10;

UPDATE products
set stock = stock -1
where name = 'mouse';

DELETE FROM products where id =12;

SELECT count (*)
FROM products;

select avg(price)
FROM products;

SELECT products.name,
products.price,
categories.name
from products
JOIN categories
on products.category_id = categories.id;

SELECT * from products
where category_id =2
and stock > 10

SELECT * FROM products
where category_id =1
or category_id =3

SELECT * FROM products
where name like 'm%'

SELECT * FROM products
where name like '%e'

SELECT * FROM products
where name like '%us%'

select * from products
order by price DESC
limit 3;

SELECT *
FROM products
WHERE NOT category_id = 1;

SELECT
    categories.name,
	avg(price)
FROM products
join categories
on products.category_id = categories.id
GROUP BY category_id;

SELECT
    categories.name,
	count(*) as total_products
FROM products
join categories
on products.category_id = categories.id
GROUP BY category_id;

SELECT categories.name,
 count(*) AS total
FROM products
JOIN categories
ON products.category_id = categories.id
GROUP BY categories.name
HAVING count(*)>=3;


SUBCONSULTAS
primero se ejecuta el parentesis, trae el porcentaje de precio
SELECT *
FROM products
WHERE price > (
    SELECT AVG(price)
    FROM products
);
ejemplo 2:
todos los productos de la categoria tech
SELECT *
FROM products
WHERE category_id = (
    SELECT id
    FROM categories
    WHERE name = 'Technology'
);

SELECT *
FROM products
WHERE category_id IN (
    SELECT id
    FROM categories
    WHERE name IN ('Food', 'Home')
);